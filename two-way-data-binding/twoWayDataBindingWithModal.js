// two way data binding with modal in javascript
// Implement a model(state, element) function that establishes a two-way binding between a JavaScript object's state.value and an HTML input element's value.
// The goal is to sync changes in state.value with the input field and vice versa.
// When the input field value changes, it should automatically update state.value, and when state.value is updated, it should reflect in the input field.
// This question is essential for mastering data binding techniques commonly used in frameworks like React and Angular.

// Example
// const state = { value: 'Hello' };
// const element = document.createElement('input');
// model(state, element);
// state.value is synced with the input field
// console.log(element.value); // 'Hello'

function model(state, element) {}
